__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Compiled native extension (built by maturin) and Rust build artifacts.
# rust/Cargo.lock IS committed, for reproducible builds.
*.so
*.pyd
*.dylib
rust/target/

# Local materialized corpora and run outputs
.synbiotorch_cache/
runs/
runs_*.log
*.parquet
wandb/

# Locally normalized/converted data
examples/data/normalized/

# Locally downloaded third-party datasets (not redistributed; see prepare scripts)
data/

# Environment
.env
.env.*

# Private planning artifact (kept out of commits, like the sibling repos)
PLAN.md
